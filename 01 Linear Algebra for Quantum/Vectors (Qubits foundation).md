
* Column vectors
* Complex numbers
* Norm (length)
* Normalization
* Inner product (dot product)

📌 Quantum mapping:

|0⟩ = [1, 0]
|1⟩ = [0, 1]

* Superposition = linear combination

Python focus

import numpy as np
zero = np.array([[1], [0]])
one = np.array([[0], [1]])

